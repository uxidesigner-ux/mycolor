{
  "name": "moi-daily-stylist",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "preview": "node scripts/serve.mjs dist",
    "preview:product": "npm run build && node scripts/serve.mjs dist",
    "check": "node --check app.js && node --check scripts/build.mjs && node --check scripts/serve.mjs && node --check scripts/setup-git.mjs",
    "verify": "npm run check && npm run build",
    "setup:git": "node scripts/setup-git.mjs"
  }
}
